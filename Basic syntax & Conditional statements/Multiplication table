#include <iostream>
#include<string>
#include<cmath>
using namespace std;

int main()
{
    int num;
    cin >> num;

    for (int i = 1; i <= 10; i++)
    {
        cout << num << " X " << i << " = " << i * num << endl;
    }

    return 0;
}
