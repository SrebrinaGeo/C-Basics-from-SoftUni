#include <iostream>
#include <string>
#include <climits>
using namespace std;

int main()
{
	string input;
	cin >> input;
	int minimum = INT_MAX;

	while (input != "Stop")
	{
		int number = stoi(input);

		if (number < minimum)
		{
			minimum = number;
		}
		cin >> input;
	}

	cout << minimum << endl;
	return 0;
}
