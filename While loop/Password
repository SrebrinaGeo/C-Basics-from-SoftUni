#include <iostream>
#include <string>
using namespace std;

int main()
{
	string username, pass;
	cin >> username >> pass;	
	string currentPass;
	cin >> currentPass;

	while (currentPass != pass)
	{
		cin >> currentPass;
	}

	cout << "Welcome " << username << "!" << endl;
	return 0;
}
